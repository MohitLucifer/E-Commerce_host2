# Base URL for the backend API consumed by the client (Create React App).
# CRA only exposes env vars prefixed with REACT_APP_ to the browser bundle,
# and inlines them at build time. Override per region/deployment.
REACT_APP_API_URL=https://e-commerce-host2.onrender.com
